# Provisioner

Planned home for idempotent Fargate provisioning and lifecycle orchestration.

Initial responsibilities from the architecture doc:

- seed workspace files
- bind secret references
- launch or replace runtime tasks
- persist runtime metadata
- recover failed provisioning

